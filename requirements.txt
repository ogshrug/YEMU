# Core (Linux + Windows). Prefer `pip install -e .[linux,dev]`; this file mirrors pyproject.toml.
yara-python>=4.3
aiosqlite>=0.19
reportlab>=4.0
requests>=2.31
scapy>=2.5
platformdirs>=3.0
PyYAML>=6.0
tomli>=2.0; python_version < "3.11"

# Linux host only (real VMs + GTK app)
libvirt-python>=9.0; sys_platform == "linux"
PyGObject>=3.46; sys_platform == "linux"
