# Core (Linux + Windows). Prefer `pip install -e .[linux,dev]`; this file mirrors pyproject.toml.
yara-python>=4.3
aiosqlite>=0.19
reportlab>=4.0
requests>=2.31
scapy>=2.5
platformdirs>=3.0
PyYAML>=6.0
pycdlib>=1.14
tomli>=2.0; python_version < "3.11"

# Desktop app (Windows + Linux)
PySide6>=6.6

# Linux host with libvirt only
libvirt-python>=9.0; sys_platform == "linux"
